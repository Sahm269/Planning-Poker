const initialisation = require( "./js/poker")
discribe(
    'test initialisation', () =>{
        test (' test commentaire encas derreur ', () =>{
            expect(initialisation().toBe())
        }
        )
    }
) 